import 'package:flutter/material.dart';

class DaftarPasien extends StatelessWidget {
  const DaftarPasien({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Riwayat Konsumsi')),
      body: const Center(child: Text('Riwayat Konsumsi (Coming Soon)')),
    );
  }
}
